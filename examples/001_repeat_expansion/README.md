# Example 001 — Tandem repeat expansion

Reference:

    ABCABCDEF

Observed:

    ABCABCABCDEF

Expected primitive observation:

    insertion of ABC

Expected semantic interpretations will eventually include:

    insertion(ABC)
    repeat_expansion(ABC, 2 -> 3)
